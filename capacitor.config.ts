import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'crud-mobile-ionic-updated',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
